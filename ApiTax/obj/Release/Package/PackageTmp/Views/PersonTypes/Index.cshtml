@model IEnumerable<ApiTax.Models.PersonType>

@{
    ViewBag.Title = "Index";
}

<h2>نوع حقیقی/ حقوقی</h2>

<p>
    @Html.ActionLink("ایجاد کردن", "Create",new { },new { @class="btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PersonTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PersonTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PersonTypeID })
        </td>
    </tr>
}

</table>
